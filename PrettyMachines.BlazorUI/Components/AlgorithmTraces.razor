<div class="@(Traces.Count > 0 ? "mono" : "") panel-content traces">
    @foreach (var trace in Traces)
    {
        @trace<br/>
    }
    @if (Traces.Count == 0)
    {
        <i>Empty</i>
    }
</div>

@code {
    [Parameter] public IReadOnlyList<string> Traces { get; set; } = []; 
}